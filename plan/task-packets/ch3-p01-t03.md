# Task Packet: CH3-P01-T03

## Scope

建立第三章可移植性与内部链接质量门禁。扫描代码、对比代码、第三章配置和受控第三章 Markdown；明确登记历史命中，不修改历史训练/基线实现，不创建实验产物。

## Files To Read

- `AGENTS.md`
- `plan/implementation-plan.md` 的 `CH3-P01-T03`
- `plan/experiment-protocol.md`
- `plan/stage-gates.md`
- 当前 CH3 任务包、配置和源码树

## Files Allowed To Edit

- `code/tests/ch3/test_portability_policy.py`
- `plan/task-packets/ch3-p01-t03.md`
- `plan/implementation-plan.md`
- `plan/progress.md`

## Required Artifacts

- 可复跑策略测试，覆盖 `code/`、`compare_code/`、`configs/ch3/` 和第三章受控文档。
- 精确 legacy allowlist：每一项包含相对文件、行、命中内容、原因和清除 Phase。
- 对新 CH3 代码、配置和受控文档的零硬编码限制；受控文档中的仓库内部链接必须相对。

## Rejection Checks

- 未列入 allowlist 的机器路径、固定主机、账号、端口、远程地址或绝对内部链接。
- allowlist 条目缺少清除 Phase/原因，或不再对应实际历史命中。
- 任一新 CH3 文件进入 allowlist。
- 修改历史训练器、基线、数据集、模型、checkpoint、prediction、metrics 或 `result/`。

## Review Gates

1. 规格符合性：扫描范围、例外身份和第三章文档链接规则符合 T03。
2. 质量：检测不把检查命令中的 regex 字面量误判为运行时硬编码；allowlist 能发现新增、删除或移动的历史命中。

## Validation Commands

- `PYTHONPATH=code python -m pytest code/tests/ch3/test_portability_policy.py -q`
- `PYTHONPATH=code python -m pytest code/tests/ch3/test_runtime_paths.py code/tests/ch3/test_ch3_config.py code/tests/ch3/test_portability_policy.py -q`
