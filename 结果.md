# H2 六条件实验结果

日期：2026-09-23  
模型：`kst_light_v2`，variant `m2_tune_lr3e4_cosine_ep80`。本轮是单 seed pilot。

## 模型架构

```text
历史值 X、时间 T、观测 mask M、context
  → HierarchicalMissingnessKAFEncoder
  → 两层 MissingSensorMixer（替换 FLA）
  → QueryConditionAdapter
  → residual decoder
  → 最近观测值 anchor + 预测 delta
```

主编码器保留原始 mask，仅使用有效观测值编码历史，并提取 freshness、可用性等缺失特征。Mixer 在传感器 token 间进行带缺失感知的交互；输出进入未来时间/通道条件适配器和残差预测头。模型有 **240,218 个参数**。

## 实验配置

MetroPT-3 chronological 50/20/30；seed `2026`；history/prediction/stride=`168/24/60`；batch size `128`；80 epochs；AdamW，learning rate `3e-4`、weight decay `1e-4`；cosine scheduler。每个条件独立训练，按 validation MAE 选择 checkpoint，再进行一次 test 评估。

## 六条件结果

| 条件 | 参数量 | Best val MAE（epoch） | Test MAE | Test RMSE | 训练时间（秒） | 推理时间（秒） | 判定 |
|---|---:|---:|---:|---:|---:|---:|---|
| `random@0.00` | 240,218 | 0.177465（61） | 0.176699 | 0.513604 | 4612.08 | 20.509 | 完成 |
| `random@0.30` | 240,218 | 0.167202（80） | 0.202384 | 0.661376 | 2293.36 | 8.476 | 完成 |
| `random@0.70` | 240,218 | 0.233563（61） | 0.210244 | 0.515466 | 2219.27 | 8.878 | 完成 |
| `low_rate@0.30` | 240,218 | 0.174993（70） | 0.211600 | 0.574708 | 2152.02 | 8.104 | 完成 |
| `block_offline@0.30` | 240,218 | 0.301725（53） | 0.285840 | 0.625130 | 2242.00 | 8.793 | **通过** |
| `mixed@0.30` | 240,218 | 0.211552（68） | 0.226631 | 0.564804 | 2242.24 | 8.284 | 完成 |
| **六条件平均** | **240,218** | **0.211083** | **0.218900** | **0.575848** | **2626.83** | **10.507** | — |

`block_offline@0.30` 按本轮要求判为通过。H2 六条件平均 test MAE 为 `0.218900`；与 FF+GRU 记录的平均 MAE `0.2293` 相比，低约 **4.54%**。以上为单 seed 结果。

## 配置与结果位置

- 首个 mixed 条件配置：`configs/pilot/metropt3/scheme_b_point_m2_tune_lr3e4_cosine_ep80_matrix.yaml`
- 其余五个条件配置：`configs/pilot/metropt3/scheme_b_point_m2_tune_lr3e4_cosine_ep80_remaining5_matrix.yaml`
- 六条件总配置：`configs/pilot/metropt3/scheme_b_point_m2_tune_lr3e4_cosine_ep80_all6_matrix.yaml`
- 六个结果目录位于 `result/pilot/metropt3/runs/`，每个目录含 checkpoint、metrics、history、predictions 和 manifest。
