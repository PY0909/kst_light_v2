# M2 H2 Cosine Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Evaluate M2 with learning rate 3e-4 and cosine annealing for 80 epochs on MetroPT-3 mixed@0.30.

**Architecture:** Keep `kst_light_v2` and the two-layer missing-sensor mixer unchanged. Add one isolated pilot matrix and registry entry using `learning_rate=3e-4`, `scheduler=cosine`, and `epochs=80`; validation-best checkpoint remains the only checkpoint used for test.

**Tech Stack:** Python 3.9, PyTorch 2.5.1 CUDA, YAML matrix runner, pytest.

---

### Task 1: Add isolated H2 matrix and CLI registry

**Files:**
- Create: `configs/pilot/metropt3/scheme_b_point_m2_tune_lr3e4_cosine_ep80_matrix.yaml`
- Modify: `code/run_scheme_b_matrix.py`

- [ ] Create a matrix with seed/split_seed/mask_seed 2026, mixed@0.30, batch 128, hidden 64, lr 0.0003, scheduler cosine, epochs 80, and variant `m2_tune_lr3e4_cosine_ep80`.
- [ ] Register key `point_m2_tune_lr3e4_cosine_ep80` in `MATRIX_PATHS`.
- [ ] Run dry-run and assert one new scientific key.

### Task 2: Verify and run H2

**Files:**
- Read: generated manifest, metrics, history, checkpoint.

- [ ] Run py_compile and targeted identity/mixer tests.
- [ ] Launch local CUDA full run with 4 workers and progress output in a persistent session.
- [ ] Confirm epoch logs include train loss, validation MAE, learning rate, and best flag.
- [ ] Confirm completion status, one test evaluation, and validation-best checkpoint.

### Task 3: Analyze H2

- [ ] Compare H2 against M2 lr=2e-4/50, M2 lr=3e-4/50, and lr=1e-4/80.
- [ ] Report validation MAE, test MAE, test RMSE, parameter count, train/inference time, checkpoint, and pilot-only conclusion.
