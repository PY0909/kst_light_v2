"""Fair comparison baselines on the unified pilot interface (CH2.5-P02).

Submodules keep honest implementation identities: every model class carries
``IMPLEMENTATION`` (``faithful``/``adapted``), ``SOURCE_IDENTITY`` (plain-text
citations, no URLs), ``REQUIRES_TIME_INPUT`` and an ``ADAPTER`` description so
the registry can report what each baseline really is.
"""
